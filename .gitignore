# Build output
build/
*.class
robofight.apk
robofight.apk.idsig

# Signing — NEVER commit the keystore (holds the private key)
robofight.keystore
*.jks

# Gradle (if it ever returns)
.gradle/
local.properties

# OS / editor
.DS_Store
Thumbs.db
*.swp

# stray dex/build artifacts
*.dex

# Android Studio caches (never commit)
.idea/caches/

# scratch / debug artifacts
build.log
shot.png
window_dump.xml
