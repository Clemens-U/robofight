# Build output
build/
*.class
robofight.apk
robofight.apk.idsig

# Signing — NEVER commit the keystore (holds the private key) or its password
robofight.keystore
robofight.keystore.backup
keystore.pass
*.jks

# Gradle (if it ever returns)
.gradle/
local.properties

# OS / editor
.DS_Store
Thumbs.db
*.swp

# stray dex/build artifacts
*.dex

# Android Studio IDE config (per-machine, never commit)
.idea/

# scratch / debug artifacts
build.log
shot.png
window_dump.xml
